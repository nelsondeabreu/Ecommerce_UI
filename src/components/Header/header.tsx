import { Text, View } from "react-native";


export const Header = () => {
    return (
        <View>
            <Text style={{ fontSize: 18, fontWeight: "bold" }}>Header</Text>
        </View>
    );
}